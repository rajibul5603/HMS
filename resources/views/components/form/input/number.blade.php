<div>
    <div class="form-group">
        <label for="{{$id ?? ''}}">{{$label ?? ''}}</label>
        <input type="number" class="form-control {{$class ?? ''}}" id="{{$id ?? ''}}" name="{{$id ?? ''}}" placeholder="{{$placeholder ?? ''}}" value="{{$value ?? ''}}">
      </div>
</div>
